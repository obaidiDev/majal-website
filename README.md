# موقع مبادرة مجال · Majal Initiative Website

موقع تعريفي يعرض أهداف مبادرة مجال وأثرها وإنجازاتها. الموقع عربيّ بالكامل
(RTL) ومبنيّ على React + Vite، وجميع المحتوى مُخزّن في ملفات YAML سهلة التحرير،
ومُهيّأ للنشر على **GitHub Pages**.

> الشعار: **نفتح لك آفاق التقنية — Unlocking your tech horizons**

## البنية

```
data/            ← كل محتوى الموقع (YAML) — حرّره هنا دون لمس الكود
  site.yaml          معلومات المبادرة والروابط والقائمة
  goals.yaml         المشكلة، الحل، الأهداف، الرؤية، المستفيدون
  impact.yaml        الأرقام ونتائج الاستبيانات (الأثر والفعالية)
  programs.yaml      البرنامج الصيفي والورش والنسخة القادمة
  testimonials.yaml  آراء المستفيدين
src/             ← مكوّنات React والأنماط (هوية مجال البصرية)
source-data/     ← الملفات الخام (استبيانات CSV/TXT، دليل الهوية PDF) للمرجع
.github/workflows/deploy.yml  ← نشر تلقائي إلى GitHub Pages
```

## التشغيل محليًا

```bash
npm install
npm run dev        # خادم تطوير على http://localhost:5173
npm run build      # بناء الإنتاج إلى dist/
npm run preview    # معاينة نسخة الإنتاج
```

## تحديث المحتوى

كل الأرقام والنصوص والبرامج والآراء موجودة في مجلد `data/` بصيغة YAML.
عدّل القيمة واحفظ — لا حاجة لتعديل الكود. عند الدفع إلى الفرع `main` يُعاد
بناء الموقع ونشره تلقائيًا.

## النشر على GitHub Pages

1. ارفع المشروع إلى مستودع على GitHub (الفرع `main`).
2. من **Settings → Pages → Build and deployment**، اختر **GitHub Actions**.
3. كل دفعة إلى `main` تبني وتنشر الموقع تلقائيًا عبر `.github/workflows/deploy.yml`.

### النطاق المخصص (Custom domain)

الموقع يُخدَّم من جذر النطاق المخصص **majal.website**:

- ملف `public/CNAME` يحتوي على النطاق ويُنسخ تلقائيًا إلى مخرجات البناء.
- مسار البناء (`base`) مضبوط على `/` في `vite.config.js` وسير العمل.
- إعدادات DNS لدى مزوّد النطاق (Namecheap):
  - سجلات **A** للنطاق الجذر `@` تشير إلى عناوين GitHub Pages:
    `185.199.108.153` · `185.199.109.153` · `185.199.110.153` · `185.199.111.153`
  - (اختياري) سجل **CNAME** لـ `www` يشير إلى `obaididev.github.io`

> لو رجعت لاحقًا إلى مسار فرعي (`username.github.io/repo`)، ابنِ بـ
> `BASE_PATH=/repo/ npm run build` واحذف ملف `CNAME`.

## الهوية البصرية

مستمدة من دليل هوية مجال:

| العنصر | القيمة |
|--------|--------|
| التركوازي (Teal) | `#28c2c6` |
| الأزرق البترولي (Navy) | `#0a5374` |
| الأصفر (Yellow) | `#f7c948` |
| الرمادي (Gray) | `#cccccc` |
| الخط العربي | IBM Plex Sans Arabic |
| الخط اللاتيني | Montserrat (بديل عن Avenir) |

> ملاحظة: أكواد الألوان في ملف الهوية (PDF) كانت لا تطابق العيّنات/الشعار
> المعروضة (كُتب «Purple #9a66ff» بينما العيّنة تركوازية)، لذلك اعتُمدت الألوان
> الفعلية الظاهرة في الشعار وجميع التطبيقات.
